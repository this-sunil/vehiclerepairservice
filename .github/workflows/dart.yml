name: Flutter CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  # --- Android Build ---
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          # Caching ko false rakhein agar 'already watching' error aa raha hai
          cache: false 

      - name: Create .env
        run: echo "${{ secrets.ENV_FILE }}" > .env

      - name: Clean & Get Dependencies
        run: |
          flutter clean
          flutter pub get

      - name: Build APK
        # Ismein --no-pub flag use karein taaki build dobara pub get na chalaye
        run: flutter build apk --release --no-pub --build-number=${{ github.run_number }}

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: android-release
          path: build/app/outputs/flutter-apk/app-release.apk

  # --- iOS Build ---
  build-ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: false

      - name: Create .env
        run: echo "${{ secrets.ENV_FILE }}" > .env

      - name: Clean & Get Dependencies
        run: |
          flutter clean
          flutter pub get

      - name: Install Pods
        working-directory: ios
        run: |
          rm -rf Pods
          rm -rf Podfile.lock
          pod install --repo-update

      - name: Build iOS (No Codesign)
        run: flutter build ios --release --no-codesign --no-pub --build-number=${{ github.run_number }}

      - name: Package IPA
        run: |
          mkdir -p Payload
          cp -R build/ios/iphoneos/Runner.app Payload/
          zip -r ios-release.ipa Payload
          
      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: ios-release
          path: ios-release.ipa
